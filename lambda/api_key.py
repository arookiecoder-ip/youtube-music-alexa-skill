# This file is tracked in git as a placeholder template. Never commit real
# values here — fill them in only in the copy pasted into the Alexa console
# code editor (Code tab), which is not part of this repo.
API_KEY = "<same value as API_KEY in .env / ytmusic.service>"
DEFAULT_API_URL = "<https://your-public-domain, matching PUBLIC_BASE_URL>"
